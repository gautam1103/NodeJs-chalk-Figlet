// os feature
let os = require("os");


// console.log(os.arch());
// console.log(os.platform());

//console.log(os.networkInterfaces()); // returns the information about my connected wifii like IP address

console.log(os.cpus()); // It returns the load on the server


